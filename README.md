# 🚀 Python Data & Web Boilerplate (Dockerized)

แม่แบบโปรเจกต์ (Template) สำหรับงานสาย Automation, Data Analysis และ Web Development ครบวงจรในชุดเดียว พัฒนาขึ้นเพื่อช่วยให้การเริ่มต้นโปรเจกต์ใหม่ทำได้รวดเร็วและเป็นระบบ รองรับทั้งการทำ Scraping, Database Management และการเชื่อมต่อ Google Sheets API

## 🛠 Tech Stack ภายในชุดนี้
- **Flask:** สำหรับทำ Web Application หรือ REST API
- **Jupyter Notebook:** สภาพแวดล้อมสำหรับการวิเคราะห์ข้อมูลและเขียนสคริปต์
- **PostgreSQL 15:** ฐานข้อมูล Relational สำหรับเก็บข้อมูลหลักของระบบ
- **Google API Integration:** รองรับการอ่าน/เขียน Google Sheets อัตโนมัติ
- **Docker & Docker Compose:** จัดการ Environment ให้รันได้เหมือนกันทุกเครื่อง

---

## 📂 โครงสร้างโปรเจกต์
- `/flask`: โค้ดฝั่ง Web Application และ API
- `/jupyter`: สภาพแวดล้อมสำหรับ Data Science และสคริปต์ Automation
- `/notebooks`: พื้นที่เก็บไฟล์ `.ipynb` (เชื่อมต่อ Volume ไว้แล้ว ข้อมูลไม่หาย)
- `docker-compose.yml`: ไฟล์ควบคุมการทำงานของทุก Container
- `requirements.txt`: รายการ Library ทั้งหมดที่ติดตั้งในระบบ

---

## 🚀 เริ่มต้นใช้งาน (Quick Start)

### 1. เตรียมความพร้อม
ตรวจสอบให้แน่ใจว่าเครื่องของคุณติดตั้ง **Docker** และ **Docker Compose** เรียบร้อยแล้ว

### 2. ตั้งค่าคอนฟิก (.env)
สร้างไฟล์ `.env` ที่ Root ของโปรเจกต์ เพื่อกำหนดค่าพื้นฐาน:
```env
DB_USER=admin
DB_PASSWORD=your_password
DB_NAME=mydatabase
JUPYTER_TOKEN=docker